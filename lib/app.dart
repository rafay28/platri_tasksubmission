import 'package:flutter/material.dart';

import 'company/view/company_page.dart';

class CompanyProfileApp extends MaterialApp {
  const CompanyProfileApp({Key key}) : super(key: key, home: const CompanyPage());
}